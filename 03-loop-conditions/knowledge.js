// 1. == will compare the value disregarding data type, === is strict and will only compare same data types.
// 2. Ternary operator is a short hand code for a if else Condition
// 3. foo will be undefined because they are not the same data type
// 4. u would use a switch to check against multiple conditions, u would use a multi if/else if u want sequential
// 5.null

(1 || 0) || "" && [].length;
